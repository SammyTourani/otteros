[package]
name = "otter-net-proto"
version = "0.1.0"
edition = "2024"
publish = false
description = "OtterOS network stack protocol encoders/decoders: Ethernet, ARP, IPv4, ICMP, UDP, DHCP, DNS. no_std+alloc, zero dependencies (DECISIONS.md D2, D27)."
license = "MIT OR Apache-2.0"

# DECISIONS.md D27: pure crates in crates/ carry zero external dependencies.
[dependencies]

[dev-dependencies]

[lib]
name = "otter_net_proto"
path = "src/lib.rs"
