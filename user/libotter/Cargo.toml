[package]
name = "libotter"
version = "0.1.0"
edition = "2024"
publish = false
description = "OtterOS userspace runtime: _start, syscalls, allocator, panic handler (brief M2-T3)."

[lib]
test = false
doctest = false
